
params {
    outdir = 'results'
    tracedir = "${params.outdir}/pipeline_info"
    publish_dir_mode = 'copy'
}

docker {
    enabled = true
    runOptions = '--platform linux/amd64'
}

process {
    cpus = 1
    memory = '1.GB'
    time = '1.h'
}
