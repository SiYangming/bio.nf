params {
    outdir = "results"
}

process {
    cpus = 2
    memory = 6.GB
    time = 2.h
}

profiles {
    docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g) --platform=linux/amd64'
    }
}
